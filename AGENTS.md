# AGENTS.md

Guidance for AI agents working in this repo.

## Project state

Backend is fully built and live in production (Supabase — Postgres/PostGIS, Auth, Realtime, Storage, Edge Functions; see docs/backend-plan.md, all 10 slices shipped — but read its 2026-09-07 amendment: everything that doc calls "stubbed" or "deferred" has moved, and `ai-proxy` runs on **Gemini**, not Anthropic). `ui/` is a complete Expo Router app wired to real data — no mocked data remains. Major UI rebuilds (Discover, My Games, bottom nav, Discover map, auth/onboarding) are shipped; see docs/discover-plan.md, docs/my-games-plan.md, docs/nav-plan.md, docs/map-plan.md, docs/auth-onboarding-plan.md. The whole app was then redesigned again — docs/v2-design-plan.md (P0–P8, all shipped) is the authority on design tokens and type, and docs/design-brief.md drives the v3 per-screen passes (Prompts 5–8 shipped). App is in private beta (Sydney, **iOS via TestFlight only**). **Android has not shipped** — corrected 2026-09-07; "Android added in batches" was aspirational. The signing half is done (release keystore + all four `ANDROID_*` secrets) and `build-android.yml` produces a signed apk/aab on `workflow_dispatch`, but the Play Store listing and the physical-device verification step are still in progress. Treat every beta metric as an **iOS-only read**; gtm-plan §3.1 G2 is still the top launch blocker. Tech stack decided (see docs/tech-stack.md) — don't change it unprompted, ask first.

**Build pipeline:** iOS and Android both ship from self-managed GitHub Actions, **not EAS** (`.github/workflows/build-ios.yml`, `build-android.yml`). EAS is used only for OTA JS updates.

Several plan docs still carry a "proposed" header for work that shipped. A docs drift audit on 2026-09-07 annotated every one it found; each amendment is a dated block at the top of the affected section. If a doc's header and its body disagree, the dated amendment is the current read.

## What this app is

SMASHIO — player-matching app for Australia, badminton first, multi-sport by design. Core action: find/match with players for a game. Not a venue-booking app; venue booking confirmation is just a trust signal. iOS/Android app only — website (smashio.com.au, `website/`) is marketing, store links, and read-only SEO/share pages for games, venues and clubs (`website/api/*`, added 2026-08-31 by gtm-plan G3/G11 and social-plan C0). Still **no in-app functionality on web** — nothing there lets you join or host.

Read [README.md](README.md), [docs/mvp-spec.md](docs/mvp-spec.md), [docs/business-context.md](docs/business-context.md), [docs/tech-stack.md](docs/tech-stack.md) before proposing features. Backend/integration work: read [docs/backend-plan.md](docs/backend-plan.md) first. Before touching Discover, My Games, the bottom nav, the map, or auth/onboarding, read that area's plan doc (discover-plan.md / my-games-plan.md / nav-plan.md / map-plan.md / auth-onboarding-plan.md) — the diagnosis and "not doing" sections explain why the code looks the way it does.

[docs/venues-plan.md](docs/venues-plan.md) (facility directory — amenities, pricing, venue detail screen) got explicit sign-off 2026-08-15 and A1-A6 are live: schema, `venue_detail`/`venues_near` RPCs, `ui/app/venue/[id].tsx`. All 37 P1 leads enriched 2026-08-15 (56 venues at that point, up from the original 8-row seed) **and the P2 queue was finished 2026-08-17** — corrected 2026-09-07, this line said "the 51-venue P2 queue is still unenriched" for three weeks after it was done. 46 of 51 P2 rows were real, 21 new venues inserted and 9 upgraded (`20260817000200_p2_enrichment.sql`), so the **directory covers ~98 venues, not 56**. Anywhere you see "56 venues" in gtm-plan.md or quick-wins.md, that is the 2026-08-15 number. What is left: 238 untriaged P4-bucket Places-sweep leads, the deferred amenity-filter UI (A4) and the photo moderation queue UI (A5) — see venues-plan.md §8. [docs/post-game-plan.md](docs/post-game-plan.md) got sign-off 2026-08-24 and is implemented: the host now occupies one of `max_players`, reserved spots can be named/invited/claimed, the host marks no-shows, ratings carry a player/host dimension plus an explicit skill vote, and ratee-side rating reads are aggregate-only. Read it before touching `ratings`, `rating_tags`, `game_players`, `games.reserved_spots`, or capacity math — the diagnosis section lists six ways the old flow was broken.

[docs/smashimals-plan.md](docs/smashimals-plan.md) got direction sign-off 2026-08-25 and **the app half is implemented** (corrected 2026-09-07 — this line said "not yet implemented" and was overtaken by `d5ac3ab` on 2026-08-26): prop overlays that let all 28 shipped bust avatars hold a banner/trophy (`ui/components/PropOverlay.tsx`, six props in `ui/assets/props/`), the 4-character full-body cast for empty states (`EmptyState.tsx` takes a `character` prop; six keys ship), and the layered-PNG puppet rig (`ui/components/SmashimalRig.tsx`) — but the rig shipped **reduced to idle sway + blink**, the quokka arm wave and celebrate were dropped 2026-08-26 for a broken shoulder seam. **The website half (W0/W1) is not built** — no cast art in `website/`, no `404.html`. Read it before touching `EmptyState.tsx`, the wizard/post-game success screens, or `website/`. Its §6 records four settled decisions — no reliability mood states, galah never aimed at a real player — don't reopen them; note §6's third decision (brand mark untouched) was **reversed 2026-09-07** by the new mark, and is annotated in place.

[docs/social-plan.md](docs/social-plan.md) (rewritten 2026-08-31 as the **community + platform plan**) is **partially signed off** — read §17 first, it records ten decisions and overrides anything in the body still phrased as a proposal. Approved to build: a **text feed with the composer** (B0+B0.5+B1+B2+B5+B7+N1, §13.1, ~12.5 d; sequence in §13.6) and clubs seed-only (C0, §13.3). Images (`post_media`), comments and reactions are cut from v1 and held in B3. Deferred entirely: booking outreach (§12). **All of §13.1 and C0 have since shipped** (2026-08-31 → 2026-09-01) and then had a v3 design pass, which reversed two halves of decision 3 in opposite directions: plain `text` posts were **cut entirely** (`20260901110000_drop_text_posts.sql` — a player creates only `looking_for_players` and `question`), while **replies and reactions shipped** (`20260901100000_feed_v3_replies_reactions.sql`). Images are still cut and B3 is still the gate. See §17's 2026-09-07 amendment before writing anything about feed scope. Slice N1 changes the bottom nav: **Chat merges into My Games and the feed takes the freed tab slot** (`Discover | Feed | My Games | Profile`, still four items) — read social-plan §13.5, my-games-plan §6.1 and nav-plan's 2026-08-31 amendment together before touching the tab bar, and don't re-propose moving My Games under Profile (§13.5 records why it was rejected). Note §13.2 — a read-only checkpoint was approved and reversed the same day; don't re-propose that either. The reason matters: **App Store 1.2 already applies to this app via chat**, so posts widen an existing obligation rather than crossing a new threshold, and B5 (content reporting + `ai-proxy` text filter) is 2 days, not a wall. Everything else in the doc is still proposed and needs sign-off before code or a migration lands. Read its §0 before touching anything social: `blocks`, `user_reports` and `profile_visibility` shipped in `20260822000000_profile_settings.sql` in a *different shape* than the doc originally proposed, and §0 is the reconciliation. §1 records the positioning call behind the widened "everything badminton in Sydney" ambition — **roadmap widens, launch message stays narrow**, so gtm-plan's "never say book courts" anti-message still stands. §2 is the ten-surface map with build/aggregate/never calls, §7 designs clubs as a real entity (closing gtm-plan G12), and §12 is the booking ladder. Don't propose gear marketplaces, pro-content feeds, or in-app booking without reading §16 first.

[docs/gtm-plan.md](docs/gtm-plan.md) (Sydney go-to-market for v1 public, written 2026-08-26) is **proposed, not signed off**. Its §3 is a marketing-leverage audit of the app — the 15 gaps (G1–G15) that make acquisition leak, each with repo evidence and a pointer to the plan doc or quick-win item that owns the fix. Read §3 before proposing growth/marketing work so you don't re-derive a gap; §4–§7 (channels, 90-day sequence, budget) need explicit sign-off before money is spent.

[docs/quick-wins.md](docs/quick-wins.md) is a running backlog of ≤1-day items (missing OTA updates, missing Android `assetlinks.json`, no analytics, no waitlist, etc.), each with the gap evidence. Not a plan doc, not approved, not ordered work — check it before proposing a "small improvement" so you don't re-derive one that's already listed, and add to it rather than starting a new doc.

The venues-plan §3 discovery sweep HAS been run (2026-08-15) — `scripts/venues/` + `data/venues/`, results in [data/venues/SWEEP-FINDINGS.md](data/venues/SWEEP-FINDINGS.md). Read it before touching venue data: it found that `seed.sql`'s "NBC Homebush" is stale, that `venues.google_place_id` uniqueness does not stop duplicate venues (NULL place_id on the 8 seeded rows), and that venue matching must never merge on proximity alone.

Testing locally: log in with `test@smashio.dev` / `Test1234!` (email/password form, no Google needed). Local dev/e2e run against the local `supabase start` db by default (`ui/.env`, seeded by `supabase/seed.sql`) — the same account also exists on the hosted project with bot accounts + games for manual hosted testing, see backend-plan.md's "Test data & local login" section.

## Rules

- Sport must stay a config/data concern, not hardcoded — badminton ships first but engine assumes more sports later.
- Don't add scope beyond the MVP spec (docs/mvp-spec.md) without asking.
- Business/legal facts (ABN, ASIC name status) belong in docs/business-context.md — keep updated if they change, don't duplicate elsewhere.
- Stack is decided (docs/tech-stack.md: React Native/Expo + Supabase + Google Maps, chat in-house on Supabase Realtime, AI calls server-side only). Changing it needs explicit user sign-off.
- AI features must go through a server-side proxy — never call the LLM API directly from the client app.
- Before touching the iOS runner image, the Xcode version, or the `expo-modules-jsi` pin/patch in `ui/package.json`, read [store-readiness-plan.md](docs/store-readiness-plan.md) §"iOS runner image / Xcode / expo-modules-jsi". Those three are one coupled decision: changing them independently shipped two builds (1077, 1079) that passed CI and then crashed on launch with a missing Swift symbol. A green iOS build does not prove the app launches.
